types:
  CredentialInfo:
    properties:
      url: string
      token: string
      caCert: optional<string>
  CredentialReport:
    properties:
      accountId: string
      clusterName: string
      credential: optional<CredentialInfo>
      errors: optional<list<string>>