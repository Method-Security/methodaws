types:
  CredentialInfo:
    properties:
      url: string
      token: string
      caCert: optional<string>
      expiration:  optional<datetime>
  CredentialReport:
    properties:
      accountId: string
      clusterName: string
      credential: optional<CredentialInfo>
      errors: optional<list<string>>